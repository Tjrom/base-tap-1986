# Production URL (set this in Vercel: Settings → Environment Variables)
NEXT_PUBLIC_URL=https://basetap-1986.vercel.app

# Onchain leaderboard contract on Base (deploy contracts/Leaderboard.sol in Remix, then set address)
# NEXT_PUBLIC_LEADERBOARD_CONTRACT=0x...
