# Инструкция по деплою на Vercel через GitHub

## ✅ Что уже сделано

- ✅ Git репозиторий инициализирован
- ✅ Первый коммит создан
- ✅ Все файлы проекта готовы
- ✅ **URLs обновлены** (GitHub username: `Tjrom`)
- ✅ **Remote подключён** к `https://github.com/Tjrom/base-tap-1986.git`
- ✅ Ветка переименована в `main`

## 📤 Осталось сделать

### 1. Создай репозиторий на GitHub

1. Зайди на https://github.com
2. Нажми **New repository** (или **+** → **New repository**)
3. Название: `base-tap-1986`
4. Выбери **Public** или **Private**
5. **НЕ** ставь галочки на "Initialize with README", "Add .gitignore", "Choose a license" (всё уже есть)
6. Нажми **Create repository**

### 2. Запушь код в GitHub

Выполни в терминале из папки `C:\Users\anyas\base-tap-1986`:

```bash
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используй Personal Access Token вместо пароля
- Или настрой SSH ключи

**Готово!** Код будет на GitHub 🎉

## 🚀 Деплой на Vercel

### Вариант A: Через GitHub (рекомендуется)

1. Зайди на https://vercel.com
2. Нажми **Add New → Project**
3. Выбери репозиторий `base-tap-1986`
4. Vercel автоматически определит Next.js
5. Нажми **Deploy**

### Вариант B: Через Vercel CLI

```bash
# Установи Vercel CLI (если ещё не установлен)
npm install -g vercel

# Авторизуйся
vercel login

# Задеплой из папки проекта
cd C:\Users\anyas\base-tap-1986
vercel
```

## ⚙️ Настройка переменных окружения на Vercel

После первого деплоя:

1. Зайди в настройки проекта на Vercel: **Settings → Environment Variables**
2. Добавь переменную:
   - **Name**: `NEXT_PUBLIC_URL`
   - **Value**: URL твоего Vercel деплоя (например, `https://base-tap-1986.vercel.app`)
   - **Environment**: Production (и Preview, если нужно)
3. Сохрани и перезапусти деплой

## 📋 Дальнейшие шаги

После деплоя следуй инструкциям из `README.md`:
- Добавь ассеты (звуки и картинки) в `public/`
- Настрой manifest через Base Build
- Проверь через Base Preview
- Опубликуй в Base app

---

**Всё готово для пуша!** Просто создай репозиторий на GitHub и выполни `git push -u origin main` 🚀
